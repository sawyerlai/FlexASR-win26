Updated Plan:

Get FlexASR running on FPGA
Test a singular function like GeLU or ReLU -> benchmark before optimizations -> benchmark running GPU as well
Implement rotation quantization across the HLS -> benchmark
Implement memory efficient recomputation strategies -> benchmark
Check in with Thierry early next week after finishing steps 1-2
